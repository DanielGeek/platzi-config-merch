/* configuracion para obtener funciones y variables de entorno de firebase function
 Nota: se debe tener plan de pago */

// const functions = require('firebase-functions');

// FIREBAE_PAYPAL_CLIENT_ID = functions.config().paypal.react_app_paypal_client_id;
// FIREBASE_GOOGLE_MAPS_API_KEY = functions.config().google
//   .react_app_paypal_client_id;

// exports.ejemploVariableDeEntorno = functions.https.onRequest((req, res) => {
//   if (req.method === 'GET') {
//     res.send(
//       `Tu variable de entorno es ${FIREBAE_PAYPAL_CLIENT_ID} ${FIREBASE_GOOGLE_MAPS_API_KEY}`
//     );
//   } else {
//     res.send(400).end();
//   }
// });
